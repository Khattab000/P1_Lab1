#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int count(int n[]) {
	for (int n[]);
}
int main(void) {
	char* names[] = { "Alan", "Frank", "Ann", "John", "Andrew" };
	int ages[] = { 43, 23, 21, 89, 2 };
	int n;
	int i;


	for (i = 0; i <= n - 1; i++) {
		if (names[i][0] == 'A') {
			if (ages[i] >= 20) {
				printf("%s\n", names[i]);
			}
		}
	}
	return 0;  //TODO 

}